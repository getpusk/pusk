// ── Messages ──
let _lastMsgDate='';
function addMsg(m){const el=$('msgs');if(el.querySelector('.m-empty'))el.innerHTML='';
  // Day separator
  if(m.date){const d=new Date(m.date);const ds=d.toDateString();if(ds!==_lastMsgDate){_lastMsgDate=ds;const now=new Date();let label;if(ds===now.toDateString())label=lang==='ru'?'Сегодня':'Today';else{const y=new Date(now);y.setDate(y.getDate()-1);if(ds===y.toDateString())label=lang==='ru'?'Вчера':'Yesterday';else label=d.toLocaleDateString(lang==='ru'?'ru':'en',{day:'numeric',month:'long'})}el.innerHTML+=`<div class="day-sep"><span>${label}</span></div>`}}
  const who=m.sender==='bot'?(m.bot_name||$('hdr-title').textContent.replace('# ','')||'Bot'):(m.sender_name||localStorage.getItem('pusk_uname')||'You');const tm=fmtTime(m.date);
  const isBot=m.sender==='bot';const isMine=m.sender==='user'&&m.sender_name===localStorage.getItem('pusk_uname');
  const txt=m.text||'';let alertCls='';if(txt.includes('**ALERT**')||txt.includes('status: firing'))alertCls=' m-alert';if(txt.includes('**Resolved**')||txt.includes('status: resolved'))alertCls=' m-resolved';if(txt.includes('**ACK**')||txt.includes('**Muted'))alertCls=' m-acked';
  let h=`<div class="m${isBot?' m-bot':''}${alertCls}" id="m-${m.message_id}" data-sender="${esc(m.sender)}" data-sname="${esc(m.sender_name||'')}" data-mine="${isMine?1:0}"><div class="m-ava" style="background:${nameColor(who)}">${who[0].toUpperCase()}</div>`;
  if(m.reply_to){const orig=document.getElementById('m-'+m.reply_to);const qtext=orig?orig.querySelector('.m-text')?.textContent?.substring(0,40):(lang==='ru'?'Удалённое сообщение':'Deleted message');const qname=orig?orig.querySelector('.m-name')?.textContent:'';h+=`<div class="m-quote"><b>${esc(qname)}</b> ${esc(qtext)}</div>`}
  const editedTag=m.edited_at?`<span class="m-edited">(${lang==='ru'?'ред.':'edited'})</span>`:'';
  h+=`<div class="m-head"><span class="m-name">${esc(who)}</span><span class="m-time">${tm}</span>${editedTag}</div>`;
  if(alertCls===' m-alert'&&m.date){h+=`<span class="m-elapsed" data-ts="${new Date(m.date).getTime()}"></span>`}
  if(m.file_id){if(m.file_type==='photo')h+=`<img src="/file/${m.file_id}?token=${token}" style="max-width:320px;max-height:320px;border-radius:8px;margin:4px 0;cursor:pointer;object-fit:cover" onclick="window.open('/file/${m.file_id}?token=${token}','_blank')">`;else if(m.file_type==='voice')h+=`<audio controls src="/file/${m.file_id}?token=${token}" style="width:100%"></audio>`;else if(m.file_type==='video')h+=`<video controls src="/file/${m.file_id}?token=${token}" style="max-width:320px;max-height:240px;border-radius:8px"></video>`;else h+=`<a href="/file/${m.file_id}?token=${token}" target="_blank" style="color:var(--accent)">${esc(m.text||'File')}</a>`}
  h+=`<div class="m-text">${md(m.text||'')}</div>`;
  if(m.reply_markup){try{const kb=typeof m.reply_markup==='string'?JSON.parse(m.reply_markup):m.reply_markup;if(kb.inline_keyboard){h+='<div class="m-kb">';kb.inline_keyboard.forEach(row=>{h+='<div class="m-kb-row">';row.forEach(btn=>{h+=`<button class="m-kb-btn" data-cb="${btn.callback_data}" data-mid="${m.message_id}" onclick="onCb(this)">${esc(btn.text)}</button>`});h+='</div>'});h+='</div>'}}catch{}}
  if(curChan)h+=`<button class="m-del" onclick="event.stopPropagation();startReply(${m.message_id},'${escJs(who)}','${escJs((m.text||'').substring(0,40))}')">↩</button>`;
  if(curChat)h+=`<button class="m-del" onclick="onDel(${m.message_id})">x</button>`;h+='</div>';el.innerHTML+=h;if(alertCls===' m-alert')updateElapsed()}

function updateElapsed(){document.querySelectorAll('.m-elapsed').forEach(el=>{const ts=parseInt(el.dataset.ts);if(!ts)return;const sec=Math.floor((Date.now()-ts)/1000);let txt,cls;if(sec<60){txt=sec+(lang==='ru'?' сек':' sec');cls='el-ok'}else if(sec<3600){const m=Math.floor(sec/60);txt=m+(lang==='ru'?' мин':' min');cls=m<5?'el-ok':m<15?'el-warn':'el-crit'}else{const h=Math.floor(sec/3600);const m=Math.floor((sec%3600)/60);txt=h+(lang==='ru'?'ч ':' h ')+m+(lang==='ru'?' мин':' min');cls='el-crit'}el.textContent=txt;el.className='m-elapsed '+cls})}
setInterval(updateElapsed,30000);

function scrollDown(){const el=$('msgs');setTimeout(()=>el.scrollTop=el.scrollHeight,50)}

function renderPinBar(mid,text,who){$('pinned-bar').innerHTML=`<div class="m-pinned"><span onclick="document.getElementById('m-${mid}')?.scrollIntoView({behavior:'smooth'})" style="flex:1;display:flex;align-items:center;gap:6px"><b>📌</b> ${md(text.substring(0,80))}</span><span style="font-size:10px;color:var(--text2);margin:0 8px">${esc(who||'')}</span><button onclick="unpinMsg()" style="background:none;border:none;color:var(--text2);cursor:pointer;font-size:14px">✕</button></div>`}

// ── Auth ──
function auth(r){token=r.token;localStorage.setItem('pusk_token',token);localStorage.setItem('pusk_uid',r.user_id);localStorage.setItem('pusk_uname',r.username||'');if(r.role)localStorage.setItem('pusk_role',r.role);if(r.org){localStorage.setItem('pusk_org',r.org);const orgs=JSON.parse(localStorage.getItem('pusk_orgs')||'{}');orgs[r.org]={token:r.token,user:r.username,name:r.org,role:r.role||'member'};localStorage.setItem('pusk_orgs',JSON.stringify(orgs))}showApp()}
function logout(){token=null;curChat=null;curChan=null;localStorage.removeItem('pusk_token');localStorage.removeItem('pusk_uid');localStorage.removeItem('pusk_uname');localStorage.removeItem('pusk_view');if(ws)ws.close();$('landing').style.display='flex';$('auth').style.display='none';$('app').style.display='none';$('fab').style.display='none';$('settings').style.display='none';$('settings-bg').style.display='none';initLandingChat()}

// ── App views ──
async function showApp(){$('auth').style.display='none';$('app').style.display='flex';const u=localStorage.getItem('pusk_uname')||'?';$('hdr-ava').textContent=u[0].toUpperCase();$('hdr-ava').style.background=nameColor(u);$('hdr-name').textContent=u;connectWS();registerPush();await showList();const v=localStorage.getItem('pusk_view');if(v){try{const o=JSON.parse(v);if(o.t==='chat')openChat(o.id,o.n);else if(o.t==='ch')openChan(o.id,o.n)}catch{}}}

async function showList(){curChat=null;curChan=null;replyToId=0;$('reply-bar').style.display='none';$('pinned-bar').innerHTML='';$('typing-bar').style.display='none';localStorage.removeItem('pusk_view');$('main-list').style.display='block';$('chat-view').style.display='none';$('input-bar').style.display='none';$('hdr-back').style.display='none';$('hdr-title').textContent=t('main');$('hdr-online').textContent='';const isGuest=localStorage.getItem('pusk_uname')==='guest';$('fab').style.display=isGuest?'none':'flex';
  const[bots,chs,health]=await Promise.all([api('GET','/api/bots'),api('GET','/api/channels'),api('GET','/api/health')]);
  const el=$('main-list');el.innerHTML='';_channels=chs||[];
  if(chs&&chs.length){el.innerHTML+=`<div class="sec-title">${t('ch')}</div>`;for(const c of chs){const cls=c.subscribed?'sub-btn on':'sub-btn';const txt=c.subscribed?t('sub_on'):t('sub_off');el.innerHTML+=`<div class="ch-row"><span class="ch-hash" onclick="openChan(${c.id},'${escJs(c.name)}')">#</span><div class="ch-body" onclick="openChan(${c.id},'${escJs(c.name)}')"><div class="ch-name">${esc(c.name)}${c.unread>0?`<span class="ch-badge ch-badge-${c.id}" style="display:inline-block;min-width:16px;text-align:center;font-size:11px;color:#fff;background:#3db887;border-radius:8px;padding:1px 5px;margin-left:6px">${c.unread}</span>`:`<span class="ch-badge ch-badge-${c.id}" style="display:none"></span>`}</div><div class="ch-desc">${esc(c.description||'')}</div></div><button class="${cls}" onclick="toggleSub(event,${c.id})">${txt}</button></div>`}}
  if(bots&&bots.length){el.innerHTML+=`<div class="sec-title">${t('bots')}</div>`;for(const b of bots){el.innerHTML+=`<div class="bot-row" onclick="openChat(${b.id},'${escJs(b.name)}')"><div class="bot-ava" style="background:${nameColor(b.name)}">${b.name[0].toUpperCase()}</div><div><div class="bot-name">${esc(b.name)}</div><div style="font-size:11px;color:var(--text2);margin-top:2px;word-break:break-all;cursor:pointer" onclick="event.stopPropagation();if(this.dataset.show){navigator.clipboard.writeText('${escJs(b.token)}');this.textContent=t('copied');setTimeout(()=>{this.textContent='token: ${escJs(b.token)}'},2000)}else{this.dataset.show='1';this.textContent='token: ${escJs(b.token)} ('+t('tap_copy')+')'}">${t('show_tok')}</div></div></div>`}}
  if((!bots||!bots.length)&&(!chs||!chs.length))el.innerHTML=`<div class="m-empty">${t('no_items')}</div>`;
  const v=health||{};el.innerHTML+=`<div class="list-foot">${t('foot')(bots?bots.length:0,chs?chs.length:0,v.online||0)}</div>`}

async function toggleSub(ev,chId){ev.stopPropagation();const btn=ev.target;const on=btn.classList.contains('on');btn.style.opacity='0.5';await api('POST',`/api/channels/${chId}/${on?'unsubscribe':'subscribe'}`);btn.classList.toggle('on');btn.textContent=on?t('sub_off'):t('sub_on');btn.style.opacity='1'}

async function openChat(botId,name){_lastMsgDate='';localStorage.setItem('pusk_view',JSON.stringify({t:'chat',id:botId,n:name}));$('fab').style.display='none';$('pinned-bar').innerHTML='';replyToId=0;$('reply-bar').style.display='none';$('typing-bar').style.display='none';$('main-list').style.display='none';$('chat-view').style.display='flex';$('input-bar').style.display='flex';$('hdr-back').style.display='inline';$('hdr-title').textContent=name;$('msg-in').placeholder=t('msg');api('GET','/api/online').then(r=>{if(!r)return;let s='';if(r.online>0)s=r.online+' online';const away=(r.total_connected||0)-r.online;if(away>0)s+=(s?', ':'')+away+' away';$('hdr-online').textContent=s});const chat=await api('POST',`/api/bots/${botId}/start`);curChat=chat.id;const msgs=await api('GET',`/api/chats/${chat.id}/messages`);$('msgs').innerHTML='';if(msgs&&msgs.length)msgs.reverse().forEach(m=>addMsg(m));else $('msgs').innerHTML=`<div class="m-empty">${t('no_msg')}</div>`;scrollDown()}

async function openChan(chId,name){_lastMsgDate='';localStorage.setItem('pusk_view',JSON.stringify({t:'ch',id:chId,n:name}));$('fab').style.display='none';curChan=chId;curChat=null;replyToId=0;$('reply-bar').style.display='none';$('typing-bar').style.display='none';$('main-list').style.display='none';$('chat-view').style.display='flex';$('input-bar').style.display='flex';$('hdr-back').style.display='inline';$('hdr-title').textContent='# '+name;$('msg-in').placeholder='Написать в #'+name+'...';api('GET','/api/online').then(r=>{if(!r)return;let s='';if(r.online>0)s=r.online+' online';const away=(r.total_connected||0)-r.online;if(away>0)s+=(s?', ':'')+away+' away';$('hdr-online').textContent=s});api('GET','/api/users').then(r=>{if(r&&Array.isArray(r))_mentionUsers=r});const msgs=await api('GET',`/api/channels/${chId}/messages`);$('msgs').innerHTML='';const chInfo=_channels.find(c=>c.id===chId);const pinnedId=chInfo?chInfo.pinned_message_id:0;$('pinned-bar').innerHTML='';if(msgs&&msgs.length){msgs.reverse().forEach(m=>{if(!m.sender)m.sender='bot';addMsg(m)});if(pinnedId>0){const pinMsg=msgs.find(m=>m.message_id===pinnedId);if(pinMsg){renderPinBar(pinnedId,pinMsg.text||'')}}}else $('msgs').innerHTML=`<div class="m-empty">${t('no_msg')}</div>`;scrollDown();api('GET',`/api/channels/${chId}/readers`).then(readers=>{if(!readers||!readers.length)return;const myName=localStorage.getItem('pusk_uname');const lastMsgId=msgs&&msgs.length?msgs[msgs.length-1].message_id:0;const whoRead=readers.filter(r=>r.last_read_id>=lastMsgId&&r.username!==myName);if(whoRead.length>0){const names=whoRead.map(r=>r.username).join(', ');const readDiv=document.createElement('div');readDiv.className='read-receipt';readDiv.textContent=(lang==='ru'?'Прочитано: ':'Read by: ')+names;$('msgs').appendChild(readDiv)}})}

// ── Navigation ──
$('hdr-back').onclick=showList;
